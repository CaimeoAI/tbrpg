const prompt = require("prompt-sync")({sigint:false});

//! VARIABLES













//! FUNCTIONS












//! EVENT RANDOMIZER

// Creates Random Number Between 1-10

function eventGen() {
    currenEventNumber = Math.round(Math.random()*9+1)

    if(currentEventNumber === 1) {
        console.log("");
    }
}

//! MAINLOOP

console.log("WELCOME TO THE TEXT-BASED RPG!");
startGame = prompt("Do you want to start the game? [y]/[n]")

if(startGame === "y") {

    for(i=0 , i<10 , i++){
        
    }

}